export * from './tokens';
export { designSystem as qtPayDesignSystem, colors as qtPayColors, typography as qtPayTypography, radii as qtPayRadii, spacing as qtPaySpacing, componentPresets as qtPayComponentPresets } from './tokens';
